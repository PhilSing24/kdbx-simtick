# di.simtick parameters

Generated by `genparams.q` from `simtick.describe[]`; the shipped values are those of NVDA on the US large-cap market and the normal scenario (`simtick.compose` of the files in `di/simconfig`). Do not edit by hand: run `make params`.

A run reads a flat dictionary composed from four layers: the **market** file (how a market works), an **instrument** row (what makes a stock itself), a **scenario** row (what makes a day type) and the **run** dictionary (what changes between two runs). A later layer overrides an earlier one; an instrument row may override any market key. `baseintensity` is derived by `compose` from `tradesperday`.

## Instrument: the essential five

Set on the instrument row (`di/simconfig/instruments.csv`) or given to `simtick.quick` with the date.

| Parameter | Type | Description | Shipped value |
|---|---|---|---|
| `sym` | symbol | ticker | NVDA |
| `price` | float | price at the open of the (first) day | 215 |
| `drift` | float | expected annual return (annualized drift of the mid) | 0.08 |
| `vol` | float | annual volatility of the close-to-close return | 0.45 |
| `tradesperday` | long | average number of trades per day (long-run average over the day-to-day regime) | 500000 |

## Market (`di/simconfig/markets/us_largecap.json`)

Grouped as in the file. An instrument row may override any of these (XOM and PG override `spreadticks` and `primaryvenue`). The `orders` group is read by `di.simorder` and listed on its page.

### session

| Parameter | Type | Description | Shipped value |
|---|---|---|---|
| `openingtime` | minute | market open time | 09:30 |
| `closingtime` | minute | market close time | 16:00 |
| `tradingdays` | long | trading days per year, for annualizing vol and drift | 252 |
| `rngmodel` | symbol | random number source (`pseudo) | pseudo |
| `ticksize` | float | minimum price increment; quotes sit on it (0.01 for US equities) | 0.01 |
| `printgrid` | float | fraction of a tick trade prints sit on (0.1: a tenth of a tick, for midpoint and improved prints) | 0.1 |
| `clock` | symbol | clock of the diffusion: `transaction (variance grows with activity: U-shaped vol, bursts) or `calendar (variance grows with time, flat vol) | transaction |

### arrivals

| Parameter | Type | Description | Shipped value |
|---|---|---|---|
| `alpha` | float | Hawkes excitation per event | 0.3 |
| `beta` | float | Hawkes decay (must be > alpha); the branching ratio is alpha/beta | 1 |
| `profile` | float list | intraday intensity profile: positive weights, one per equal bin of the session (13 half hours), interpolated between bin midpoints | 1.6 1.2 1 0.85 0.75 0.7 0.65 0.7 0.75 0.85 1 1.2 1.8 |
| `jumpburst` | long | extra trade immigrants seeded by each jump (each with its usual cascade); 0 = none | 3000 |
| `jumpburstminutes` | float | mean delay in minutes of those immigrants after the jump | 1 |
| `quotespertrade` | float | quote updates per trade on average: quotes arrive on their own Hawkes clock at this multiple of the trade intensity | 4 |
| `quotetradelink` | float | share of the quote updates seeded by the trades (at Exp(beta) delays after them), between 0 and 1 | 0.5 |

### auctions

| Parameter | Type | Description | Shipped value |
|---|---|---|---|
| `openauctionpct` | float | opening auction print as a fraction of the day's continuous volume (0 = none) | 0.01 |
| `closeauctionpct` | float | closing auction print as a fraction of the day's continuous volume (0 = none) | 0.08 |

### sizes

| Parameter | Type | Description | Shipped value |
|---|---|---|---|
| `qtymodel` | symbol | quantity model: `mixture (round lots, blocks and irregular lots), `lognormal or `constant | mixture |
| `avgqty` | long | average trade quantity (of the irregular lots under `mixture) | 60 |
| `qtyvol` | float | quantity log volatility (lognormal and the irregular lots of the mixture) | 0.9 |
| `roundlotshare` | float | mixture: share of trades that are round lots | 0.35 |
| `roundlots` | long list | mixture: the round-lot sizes | 100 200 300 500 1000 |
| `roundlotweights` | float list | mixture: the weights of the round-lot sizes (sum to 1) | 0.5 0.2 0.1 0.12 0.08 |
| `blockshare` | float | mixture: share of trades that are blocks | 0.002 |
| `blockqty` | long | mixture: median block size | 10000 |
| `blockqtyvol` | float | mixture: log volatility of block sizes | 0.5 |

### quotes

| Parameter | Type | Description | Shipped value |
|---|---|---|---|
| `spreadticks` | float | mean bid-ask spread in ticks through the day, at least 1 (the spread is 1 tick plus a Poisson excess) | 1.15 |
| `spreadopenmult` | float | spread multiplier at the open, decaying to the midday one | 2.5 |
| `spreadmidmult` | float | spread multiplier through the day | 1 |
| `spreadclosemult` | float | spread multiplier at the close, reached by the same decay | 0.9 |
| `spreaddecayminutes` | float | minutes over which the open and close spread multipliers decay toward the midday one (e-folding time) | 15 |
| `spreadactivity` | float | exponent of local quote activity (trailing window over its expected level) multiplying the mean spread; 0 = none | 0.5 |
| `activitywindowseconds` | float | seconds of the trailing window that measures local quote activity | 60 |
| `activityclip` | float list | lowest and highest activity ratio applied to the spread | 0.25 4 |
| `avgquotesize` | long | average quote size | 500 |
| `quotesizevol` | float | log volatility of quote sizes (lognormal around avgquotesize) | 0.6 |
| `quotelot` | long | quote sizes are multiples of this lot | 100 |
| `imbalancesignal` | float | log tilt of the quote sizes toward the side of the next mid move (bid larger before a rise); 0 = none | 0.15 |

### trades

| Parameter | Type | Description | Shipped value |
|---|---|---|---|
| `sidepersistence` | float | probability a trade's aggressor side repeats the previous trade's (0.5 = independent sides) | 0.7 |
| `midpointshare` | float | share of trades printing at the midpoint | 0.12 |
| `improvementshare` | float | share of trades printing inside the touch (price improvement) | 0.08 |
| `improvementtick` | float | fraction of a tick an improved print sits inside the touch | 0.1 |
| `offexchangeshare` | float | share of trades printed off-exchange (venue TRF) | 0.42 |
| `offexchangeinside` | float | probability a midpoint or improved print is off-exchange | 0.95 |
| `venues` | symbol list | lit venues (MIC codes) trades print on | XNAS XNYS ARCX BATS EDGX IEXG |
| `venueshares` | float list | their shares of on-exchange trades (sum to 1) | 0.42 0.06 0.14 0.14 0.14 0.1 |
| `primaryvenue` | symbol | primary listing venue (MIC), where the auction prints are; NYSE names override it on their instrument row | XNAS |

### impact

| Parameter | Type | Description | Shipped value |
|---|---|---|---|
| `impactticks` | float | order-flow impact: ticks an average-size trade moves the mid in its direction (0 = none), scaled by sqrt(qty / mean size) | 0.25 |
| `impacthalflifeseconds` | float | order-flow impact: seconds over which the transient part of a trade's impact halves | 30 |
| `impactpermanent` | float | order-flow impact: share of a trade's impact that never decays, between 0 and 1 | 0.3 |

## Scenario (`di/simconfig/scenarios.csv`)

One row per day type: `normal`, `volatile`, `jumpy`. The multipliers are applied once by `compose` (then 1). The calendar group is read by `di.simmarket`.

### scenario

| Parameter | Type | Description | Shipped value |
|---|---|---|---|
| `volmult` | float | multiplies the instrument's vol (applied once by compose, then 1) | 1 |
| `volumemult` | float | multiplies the instrument's tradesperday (applied once by compose, then 1) | 1 |
| `spreadmult` | float | multiplies the mean spread in ticks (applied once by compose, then 1); the result must stay at least 1 | 1 |
| `pricemodel` | symbol | price model (`gbm or `jump) | gbm |
| `jumpintensity` | float | jump model: jumps per day | 0 |
| `jumpmean` | float | jump model: mean of the log jump size | 0 |
| `jumpvol` | float | jump model: standard deviation of the log jump size | 0 |

### calendar (di.simmarket)

| Parameter | Type | Description | Shipped value |
|---|---|---|---|
| `overnightshare` | float | di.simmarket: share of a trading day's variance that occurs overnight, between 0 and 1 (1 excluded) | 0.3 |
| `gapdayweight` | float | di.simmarket: weight of each calendar day beyond the first in an overnight gap's variance | 0.25 |
| `regimepersistence` | float | di.simmarket: AR(1) persistence of the day-level regimes, between 0 and 1 (1 excluded) | 0.7 |
| `regimecorr` | float | di.simmarket: correlation of the daily shocks to the volatility and volume regimes, between -1 and 1 | 0.7 |
| `volregimesd` | float | di.simmarket: log spread of the volatility multiplier across days, normalized so the mean daily variance is the configured one | 0.3 |
| `volumeregimesd` | float | di.simmarket: log spread of the volume multiplier across days | 0.3 |

## Run

A dictionary with any of these; the market file's `run` group carries the defaults.

| Parameter | Type | Description | Shipped value |
|---|---|---|---|
| `tradingdate` | date | the day simulated (the market file carries a default) | 2026.08.18 |
| `seed` | long | random seed (0N = unseeded; the market file carries a default) | 42 |
| `generatequotes` | boolean | return the quotes as well as the trades (they are always generated) | 1b |

## Derived

| Parameter | Type | Description | Shipped value |
|---|---|---|---|
| `baseintensity` | float | immigrant arrival rate before the profile and the cascades (trades/sec), derived by compose from tradesperday | 14.89996 |
